# Logging activities
## 03-sep-2025
2. Check the `startdate` and the `enddate` with respect to the start and end dates of the `meteo.in` file. Extract the portion of meteo.in between startdate and enddate.
3. Program at least three methods to estimate evapotranspiration. Set the optional methods in the control file. 
